export interface Country {
    name: string;
    code: string;
}

export interface CityPrimeng {
    name: string,
    code: string
}

export interface CalendarEvent {
    title: string;
    start: any;
    // Ajoutez d'autres propriétés si nécessaire
  }